{
  "name": "todo-app-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.0"
  }
}
