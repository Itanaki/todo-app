// Template column descriptions - edit these to document each column's intended use.
const columnDescriptions: Record<string, string> = {
  // Example defaults — replace these with your project-specific guidance
  todo: "(1st-Board) Add new tasks here. Use this column for tasks that are ready to be picked up.",
  "in-progress": "(2nd-Board) Tasks currently being worked on. Move here when you start actively working.",
  complete: "(3rd-Board) Finished tasks. Keep here for historical reference or cannot be edited.",
};

export default columnDescriptions;
